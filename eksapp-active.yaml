apiVersion: v1
kind: Service
metadata:
  name: eksapp-active
  namespace: eksappns
spec:
  type: LoadBalancer 
  selector:
    app.kubernetes.io/name: eksapp
  ports:
    - port: 80
      targetPort: 8080
